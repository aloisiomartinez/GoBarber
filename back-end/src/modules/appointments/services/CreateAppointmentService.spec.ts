import CreateAppointmentService from './CreateAppointmentService';

describe('CreateAppointment', () => {
  it('shoult be able to create a new appointment', () => {});

  // it('shoult not be able to create two appointments on the same time', () => {});
});
